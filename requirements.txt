# Runtime dependencies for xyz-sdr
# Versiones ancladas con ~= para builds reproducibles
# Para dev: pip install -r requirements-dev.txt
# Para AI extras: pip install -r requirements-ai.txt

# xyz-sdr — Dependencias Python
# Instala con: pip install -r requirements.txt
# Recomendado (instalador): uv pip install -r requirements.txt

# ─── Core SDR ────────────────────────────────────────────────────────────────
SoapySDR               # Abstracción hardware SDR (requiere PothosSDR instalado)
numpy~=2.1             # Operaciones numéricas / IQ data (numpy 2.1+ requiere py3.10+ y soporta cp313)
# scipy pinned to >=1.14 for cp313 wheels (scipy 1.13.x lacks py3.13 wheels)
scipy~=1.14            # Filtros, FFT, procesado de señal (compatible con numpy 2.x y cp313)

# ─── Audio ───────────────────────────────────────────────────────────────────
sounddevice~=0.4       # Reproducción de audio demodulado
soundfile~=0.12        # Lectura/escritura de archivos de audio

# ─── TUI (Terminal UI) ───────────────────────────────────────────────────────
textual~=0.60          # Framework TUI moderno (Textual)
rich~=13.0             # Estilos y colores en terminal

# ─── Configuración ───────────────────────────────────────────────────────────
tomli>=2.0; python_version < "3.11"   # TOML parser (Python < 3.11)
# tomllib es built-in desde Python 3.11
tomli_w>=1.0           # TOML writer — usado por core/config_store en runtime (import incondicional)

# Dependencias IA opcionales: requirements-ai.txt
