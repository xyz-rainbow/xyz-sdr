[pytest]
testpaths = resources/test
pythonpath = .
cache_dir = var/pytest_cache
addopts = --basetemp=var/pytest-tmp
markers =
    slow: tests that take several seconds or stress UI buffers
    integration: tests that need external services or timing-sensitive I/O
