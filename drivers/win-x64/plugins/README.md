# plugins/ — SoapySDRPlay3 (Windows x64)

Canonical location for bundled `sdrPlaySupport.dll`.

Publish from a maintainer machine:

```powershell
.\setup\install_soapy_sdrplay3.ps1 --publish-bundled
```

See [../manifest.json](../manifest.json) for SHA-256 and build metadata.
